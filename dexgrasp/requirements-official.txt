# Python packages for the official AnyDexGrasp reproduction environment.
# Use only with Python 3.8 after installing PyTorch 1.13.1 + CUDA 11.7.
numpy==1.23.5
Pillow>=9,<11
scipy==1.10.1
tqdm>=4.64,<5
tensorboard>=2.11,<3
graspnetAPI
open3d==0.18.0
xlrd2
pybullet
PyYAML>=6
opencv-python>=4.8
pyrealsense2>=2.54

# CUDA-specific components are intentionally not pip requirements:
#   pytorch==1.13.1, torchvision==0.14.1, pytorch-cuda==11.7
#   MinkowskiEngine 0.5.x (source build against the same Torch/CUDA pair)
#   third_party/AnyDexGrasp/knn (local CUDA extension)
#   third_party/AnyDexGrasp/pointnet2 (local CUDA extension)

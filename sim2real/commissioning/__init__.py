"""Explicit calibration, identification, and supervised probe tools.

Importing this package never opens hardware. Individual commands remain
dry-run or read-only by default and retain their own explicit execution gates.
"""

__all__: tuple[str, ...] = ()
